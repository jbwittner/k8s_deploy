apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-ceph-rook-operator
  namespace: argocd
spec:
  destination:
    namespace: infra-ceph-rook
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: rook-ceph
    repoURL: https://charts.rook.io/release
    targetRevision: v1.18.6
  syncPolicy:
    syncOptions:
      - CreateNamespace=true